/* multi-línea */
// una sola línea

//var nombre = "Sebastian"; // prohibido el var
let apellido = "Alarcon"; // si se puede reasignar (mutable)
const edad = 15; // se puede asignar una única vez (inmutable)
edad = 76; // ERROR!

console.log(apellido + " " + edad);